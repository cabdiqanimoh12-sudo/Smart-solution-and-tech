'use client'
import { useState } from 'react'
import Link from 'next/link'
export default function Checkout(){const [message,setMessage]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const cart=JSON.parse(localStorage.getItem('sst-cart')||'[]');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,items:cart.map((x:{id:string;quantity:number})=>({productId:x.id,quantity:x.quantity}))})});const j=await r.json();if(r.ok){localStorage.removeItem('sst-cart');setMessage(`Order ${j.orderNumber} confirmed. We’ll call you shortly.`)}else setMessage(j.error)}return <><header className="nav"><div className="container"><Link href="/" className="brand"><span className="brand-mark">S</span> Smart Solution & Tech</Link></div></header><main className="container section"><div className="form-card"><span className="eyebrow">Secure checkout</span><h1>Complete your order</h1>{message&&<div className="notice">{message}</div>}<form className="form-grid" onSubmit={submit}><label className="field">Full name<input name="customerName" required/></label><div className="form-grid two"><label className="field">Phone<input name="phone" required/></label><label className="field">Email<input type="email" name="email"/></label></div><label className="field">Delivery address<input name="address" required/></label><label className="field">Payment method<select name="paymentMethod"><option value="CASH_ON_DELIVERY">Cash on delivery</option><option value="MOBILE_MONEY">Mobile money</option><option value="BANK_TRANSFER">Bank transfer</option></select></label><label className="field">Order notes<textarea name="notes"/></label><button className="btn btn-primary">Confirm order</button></form></div></main></>}
