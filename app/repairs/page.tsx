'use client'
import { useState } from 'react'
import Link from 'next/link'
export default function Repairs(){const [result,setResult]=useState('');const [tracking,setTracking]=useState('');const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/repairs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();setResult(r.ok?`Request received: ${j.requestNumber}`:j.error)};const track=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch(`/api/repairs?number=${tracking}`);const j=await r.json();setResult(r.ok?`${j.requestNumber}: ${j.status.replaceAll('_',' ')}`:j.error)};return <><header className="nav"><div className="container"><Link href="/" className="brand"><span className="brand-mark">S</span> Smart Solution & Tech</Link></div></header><main className="container section"><div className="section-head"><div><span className="eyebrow">Repair centre</span><h1>We’ll get you back up and running.</h1><p className="muted">Tell us what’s wrong and our technicians will take it from there.</p></div></div>{result&&<div className="notice">{result}</div>}<div className="form-card"><h2>Book a repair</h2><form className="form-grid" onSubmit={submit}><div className="form-grid two"><label className="field">Your name<input name="customerName" required/></label><label className="field">Phone number<input name="phone" required/></label></div><div className="form-grid two"><label className="field">Device type<select name="deviceType"><option>Mobile phone</option><option>Laptop</option><option>Tablet</option><option>Computer</option><option>Other electronics</option></select></label><label className="field">Brand<input name="brand" required/></label></div><label className="field">Model<input name="model" required/></label><label className="field">What’s the problem?<textarea name="problem" required placeholder="Describe the issue..."/></label><div className="form-grid two"><label className="field">Preferred date<input type="date" name="preferredDate"/></label><label className="field">Image URL (optional)<input name="imageUrl" placeholder="https://..."/></label></div><label className="field">Additional notes<textarea name="notes"/></label><button className="btn btn-primary">Submit repair request</button></form></div><div className="form-card"><h2>Track your repair</h2><form className="form-grid" onSubmit={track}><label className="field">Repair request number<input value={tracking} onChange={e=>setTracking(e.target.value)} placeholder="REP-12345678" required/></label><button className="btn btn-outline">Check status</button></form></div></main></>}
